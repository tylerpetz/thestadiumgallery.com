<div id="content" class="col-sm-12 center-column content-with-background">
  <p>{{'general.404.subtext' | t}}</p>
  <div class="buttons">
    <div class="pull-right"><a href="{{shop.url}}" class="btn btn-primary">{{'general.404.continue' | t}}</a></div>
  </div>
</div>